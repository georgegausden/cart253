let raindrop = []
let threshold = 0.1;


function setup(){
  createCanvas(500,500);
  background(0);
}

function draw(){

  let randomNumber = random(0,1);

  if (randomNumber<threshold){

  }

}

function createRaindrop(){
  let raindrop = {
    x:
    y:
    vx:
    vy:
  }
}
